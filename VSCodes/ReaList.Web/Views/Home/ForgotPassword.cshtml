@model ReaList.Library.Model.UserAccounts.UserAccountModel

@{
    ViewData["Title"] = "ForgotPassword";
    Layout = "~/views/shared/_Layout.cshtml";
}

<!DOCTYPE html>
<html lang="en">

<head>
  
</head>

<body class="vh-100 body-overflow-hidden">
    <main>
        <section>
            <div class="row d-flex justify-content-center align-items-center h-100">
                <div class="col-xl-6 mt-4 mb-3">
                    <div class="card">
                        <div class="card-body pt-5 px-5 text-center">
                            <p class="text-center h1 fw-bold mb-5">Enter your Email Address to recover your account</p>
                            @if (ViewBag.ErrorMessage != null)
                            {
                                <div class="error-message text-danger">
                                    @ViewBag.ErrorMessage
                                </div>
                            }

                            <form asp-controller="Home" asp-action="ForgotPassword" method="post">
                                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                              

                                <div class="d-flex flex-row align-items-center mb-4">
                                    <div class="form-outline flex-fill mb-0">
                                        <input asp-for="Email" class="form-control" placeholder="Email" />
                                        <span asp-validation-for="Email" class="text-danger"></span>
                                    </div>
                                </div>

                                <input type="submit" value="Submit" class="btn btn-primary btn-lg px-5" />

                                <p class="text-center text-muted mt-3">
                                    Remember your Password? <a class="fw-bold text-body" asp-controller="Login" asp-action="Login"><u>Login here</u></a>
                                </p>
                            </form>
                            @section Scripts {
                                @{
                                    await Html.RenderPartialAsync("_ValidationScriptsPartial");
                                }
                            }
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </main>

  
    <footer>
        <!-- place footer here -->
    </footer>
    <!-- Bootstrap JavaScript Libraries -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/js/bootstrap.min.js"
            integrity="sha384-7VPbUDkoPSGFnVtYi0QogXtr74QeVeeIs99Qfg5YCF+TidwNdjvaKZX19NZ/e6oz" crossorigin="anonymous">
    </script>
</body>
</html>